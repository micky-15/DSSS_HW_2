from setuptools import setup, find_packages

setup(name='package_quiz',
      version ='0.1',
      description='A package for maths quiz and its unit tests',
      packages  =find_packages(),
      author = 'Bhagwinder Singh')